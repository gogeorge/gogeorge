{
  "name": "gogeorge-github-readme",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Renders a GitHub-styled profile stats card as a self-contained SVG, in matching dark and light themes.",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
